.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install commit_opener

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv commit_opener
    $ pip install commit_opener
